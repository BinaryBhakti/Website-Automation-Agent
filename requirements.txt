# Core dependencies for the Website Automation Agent
# No AI/LLM API key is required — element detection is fully deterministic.
playwright>=1.40.0     # Browser automation library
python-dotenv>=1.0.0   # Loads configuration from a .env file (optional)
