# Core dependencies for the Website Automation Agent
google-genai>=1.0.0    # Google Gemini API SDK (AI reasoning + vision)
playwright>=1.40.0     # Browser automation library
python-dotenv>=1.0.0   # Loads API key / config from a .env file
